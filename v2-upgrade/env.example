# Description: Example of .env file
# Usage: Copy this file to .env and change the values
#        according to your needs
#        Do not commit .env file to git
#        Do not change .env.example file

ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

#DB_PATH dinamically adapts in config.py, but can be set in the .env if required.
#   delete if unwanted.
DB_PATH=/absolute/path/to/store.db
#RECEIPT_PATH dinamically adapts in config.py, but can be set in the .env if required, it is a folder where .txt will save to.
#   delete if unwanted.
RECEIPT_PATH=/absolute/path/to/Receipts